/** Camera presentation stage: terminal stage, kept by every composer tier (unlike its siblings, never skipped by a lean composer). */
export { sampleSceneCameraPresentation as sampleFrameCameraPresentation } from "@/entities/scene/model/scene-camera";
