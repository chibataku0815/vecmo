# Signal Handoff — Material Wake

Status: **internal material-coupling candidate**

This isolated candidate keeps the technically closed Signal Handoff motion phrase intact
and adds one receiving knot. The existing moving aperture is the only Source
Optics owner. It drives the knot's surface, diffusion, edge, spectral, and
microstructure response; typed Source Optics tracks keep those responses at
zero before contact and wake them during arrival.

## Inspect it

1. Open the internal `signal-handoff-material` reference scene and play frames
   0–89.
2. Select **Receiving knot** to inspect its ordinary fill, stroke, and
   object-scoped grain.
3. Inspect Source Optics: **Changing aperture** is the single source and
   **Receiving knot** is its only response target.
4. Compare frames 46, 58, and 72. The response is quiet, peaks on arrival, then
   settles without a global frame effect.
5. Move the aperture or receiving knot and confirm that the surface and edge
   direction continue to derive from the same source-target relationship.

The fixture is generated-but-native: its nodes, recipe, rig, bindings, and
tracks serialize as ordinary editable Scene/Motion truth. A frozen fit/100%/
contact-sheet/A-B packet received the independent critic verdict
`request-user-review`; no automatic rejector remains. It stays internal and is
not accepted until the user explicitly judges the terminal right-side response
as a coherent internal propagation rather than a contradictory light direction.
